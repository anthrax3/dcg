@# This is a simple number listing template for demostration.
@# As you can see, these lines are comments.
@# You can render this template to see its output.
@! int count = 5;
@! for (int i = 1; i <= count; i++)
@! {
@(i)
@! }